# Superseded R — not maintained, do not run

Earlier variants of the farm model kept only so nobody rediscovers one and mistakes it for the
model. Part 7's duplication check exists for exactly this: at one point there were eight notebooks
and eleven R variants with no canonical version among them.

`Farm_research_exploration.Rmd` is the only one here that builds a model rather than plotting; the
rest are single-iteration or scratch variants.

The current pipeline is `../../stage1-python/` then `../FM Final Outputs.Rmd`.
